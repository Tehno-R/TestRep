#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	string userText;
	getline(cin, userText);
	freopen("output.txt", "w", stdout);

	cout << userText << '\n';
	return 0;
}
